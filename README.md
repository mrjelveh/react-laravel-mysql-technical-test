### React + Laravel + MySQL Technical-test


### Database

import ``` rtt.sql ``` file

### Laravel
just use ```php artisan serve``` command

### React

first run ```npm i``` command then run project with ```npm start```

